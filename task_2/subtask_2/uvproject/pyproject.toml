[project]
name = "uvproject"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Reem Ahmed", email = "reema5hmed11@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "pillow>=12.3.0",
]

[project.scripts]
uvproject = "uvproject:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
